package gwapi.entity;

/**
 * Includes all possibilities for item third types, only used for armours.
 * None is none of them, new is something that isn't in the list yet
 */
public enum ItemSubSubType {
    NONE,
    NEW,
    HEAVY,
    MEDIUM,
    LIGHT,
    CLOTHING
}
